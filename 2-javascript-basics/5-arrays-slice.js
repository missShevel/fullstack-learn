const arr = ['Apple', 'Cat', 'Banana', 'Orange'];

const part1 = arr.slice(0, arr.length / 2);
const part2 = arr.slice(arr.length / 2, arr.length);

console.log(arr);
console.log(part1);
console.log(part2);